/**
 * `/studio` page entry. The editor session host lives in
 * `StudioCuttoonEditorHost.tsx` so this file stays a thin route seam.
 */
export { StudioCuttoonEditor } from "./StudioCuttoonEditorHost";
